# Product & Tech Tracker

Purpose

- Capture improvement ideas and technical debt so we can prioritize intentionally instead of chasing every idea immediately.
- Treat this as a lightweight backlog: write down the idea, rationale, and any quick notes. Pull items into workstreams as capacity allows.

How to use

- Add new entries to the bottom with a unique identifier (e.g., `FRONTEND-01`).
- Keep notes concise: what problem it solves, rough scope/impact, and dependencies.
- When an item becomes active, move it to a dedicated design doc (see `dev-docs/design/`) or ticket system.

Backlog

- DEV-02 — Replace `npm install` with `npm ci` in Maven plugin executions for reproducible builds.
- DEV-03 — Plan the Spring Boot 4 migration before Spring Boot 3.5 OSS support ends; 3.5 keeps the app on Spring Framework 6 while security patching the current 3.x line.
- AUTH-01 — Figure out OSS-friendly auth story for static assets + APIs without Cloudflare: likely a tiny reverse proxy that terminates auth and sets a secure cookie for root-path SPA routes; dev can mimic edge by injecting `CF-Access-Jwt-Assertion`/`Authorization` headers via Vite proxy; document requirements (covers assets, avoids CSRF if cookies used) so we don’t restart from scratch.
- CI-01 — Add GH Action snippet to run `mvn -pl webapp -Pfrontend test-compile -DskipTests` (or `frontend:npm …check`) so frontend check uses the Maven-managed Node/npm.
- DOC-01 — Refresh published install/contributor docs for the current Spring Boot 3, JDK 21, MySQL 8, Maven-managed Node/npm, `webapp/frontend`, and root-path SPA workflow. Replace old `npm run start-dev` guidance with current Maven/Spring Boot and Vite commands.
- DOC-02 — Refresh auth/config docs after the embedded LDAP removal: document external LDAP URL-based setup, remove stale embedded-LDIF assumptions, and fix Spring Security 5/Spring Boot 2 wording in Spring Boot 3 docs.
- DOC-03 — Decide the docs-site tech refresh: remove or replace checked-in Bower dependencies and stale Gitter/Travis/OpenSource Box badges without breaking the static GitHub Pages build.
- FRONTEND-02a — Build container: React Query fetch/search/filter, selection state, scroll-to-selected, keyboard nav, and tanstack virtualization hooks (estimate/measure) without UI markup.
- FRONTEND-02b — Implement pure view: virtualized repos table with sticky header and inline search, selection highlight, metric link callbacks, and locales pane layout that mirrors the prototype.
- FRONTEND-02c — Locale data & accessibility: map selected repo to localeDetails or call `onLoadLocales(repoId)`, handle loading/error/empty states, add scope/aria labels and focusable selected row, and keep CSS state classes minimal.
- REVIEW-01 — Evolve review automation beyond the MVP executor: add business-day due-date handling, optional team/vendor delivery pools, and final claim strategy on top of the existing feature-based project creation, open-project exclusion, and optional translator auto-assignment.
- REVIEW-03 — Revisit review automation cron startup/Quartz lifecycle. Current persisted trigger path can instantiate `ReviewAutomationCronJob` too early during app startup; design a proper fix that matches existing Quartz patterns without changing global scheduler startup behavior opportunistically.
- WORKBENCH-03 — Decide whether `/api/textunitsBatch` should be admin-only end-to-end. The new workbench import UI is admin-gated, but backend auth still follows the broader `/api/textunits/**` permissions.
- WORKBENCH-04 — Revisit workbench import round-trip semantics for `status` and `includedInLocalizedFile`. The current batch import backend ignores those fields even though export/import can carry them.
- PLATFORM-01 — Harden the `webapp` MCP bad-translation workflow after the initial scaffold: improve review-project lineage confidence, add better operator audit visibility, and decide whether sync-only `/api/mcp` is enough before adding SSE/session support.
- PLATFORM-02 — Extend task inspection beyond single-id lookup if operators need broader triage: recent failed-task search, repository-scoped task history, and retention expectations for pollable-task input/output blobs.
- GLOSSARY-03 — Add semantic glossary retrieval on top of the current lexical/span matching so MT and AI review can retrieve consistency terms even when the exact source form differs. The current architecture keeps room for this via `MatchedGlossaryTerm`, AI-assisted extraction metadata, and per-term evidence.
- GLOSSARY-04 — Add configurable extraction noise controls and approval policies for AI-assisted glossary mining. The current extractor still uses a static stop-word list plus AI reranking; we likely want glossary-level/global ignore words, repository-specific extraction presets, and confidence thresholds.
- GLOSSARY-05 — Harden glossary candidate-term review beyond the first pass: cluster duplicate source-term proposals, add richer repository/review context for reviewers, and decide how much of the remaining legacy translation-proposal model should survive versus being retired entirely.
- GLOSSARY-06 — Turn the glossary workspace into an operational readiness layer for repository-backed terminology: readiness summary, diff/audit views, and proposal-based feedback loops. See `dev-docs/design/011-glossary-workspace.md`.
- GLOSSARY-07 — Cache compiled glossary tries for glossary-match lookups so Workbench/review detail panes do not rebuild the same repository+locale matcher on every request. Keep TTL short enough to tolerate normal backing-repo edits, and add explicit eviction on glossary write paths where practical.
- GLOSSARY-08 — Finish glossary MCP client integration: validate MCP client auth/config against `/api/mcp`, decide whether an authenticated local bridge or MCP-specific token is needed, and decide whether TM/source-glossary translation suggestions should get a reviewed write workflow beyond `bulk_upsert`. The core tools now cover glossary create/update, term list/search/review-plan/bulk-upsert/reference-link, term-index candidate seeding, glossary-to-candidate seeding, existing-term candidate linking, image upload for screenshot evidence, and TM translation suggestion. See `dev-docs/design/012-glossary-mcp.md`.
- GLOSSARY-10 — Decide the operational refresh policy for the raw term index: scheduled cadence, repository scope, retry behavior, and retention/pruning for `term_index_refresh_run` rows once automatic refreshes exist.
- GLOSSARY-11 — Harden raw term-index curation after the first glossary workspace pass: add structured metadata rendering for seeded candidates, manual split/merge review controls, duplicate/alias clustering, and richer filters for ignored/accepted suggestions. See `dev-docs/design/013-glossary-raw-term-index.md`.
- GLOSSARY-13 — Finish operational visibility for long-running term-index jobs beyond the current term-index run history: add info logs on extract/triage/candidate-generation start, finish, and failure; Micrometer counters with bounded tags such as job/result/status; timers for major phases and AI batches; avoid repository/glossary IDs as metric tags.
- GLOSSARY-14 — Add the next layer of candidate-queue sort/filter maturity now that `/settings/system/glossary-term-index/candidates` is backed by `term_index_candidate`: confidence sorting, created/updated date filters, source-type filters, and clearer distinction between candidate confidence, review confidence, and extracted-term occurrence confidence.
- GLOSSARY-15 — Finish source terminology review resolution: terminology review projects now cover glossary-backed terms and generated term candidates with specialist-input and PM-resolution phases. Candidate review projects defer glossary term creation until PM acceptance; remaining work is AI glossary assignment, cross-vendor/teamless review workflow decisions, and richer owner-side proposal edits.
- TM-01 — Monitor the `TextUnitSearcher` Hibernate Criteria rollout. The Criteria implementation is now the main `TextUnitSearcher`; the old native query builder, native query dependency, shadow comparator, and replacement package are removed. Remaining work is production SQL/load monitoring and any targeted tuning, especially around aggregated usages ordering/separators. HQL/native-SQL alternatives are documented but no longer kept in code. See `dev-docs/design/014-text-unit-searcher-replacement.md`.
- MF2-01 — Establish MF2 as a first-class Mojito program: roadmap the TMS UI, runtime libraries, shared conformance suite, and Mojito's eventual own-string migration. See `dev-docs/design/015-mf2-foundation-roadmap.md`.
- MF2-02 — Build out `mf2/conformance`: official data-model schema, source-to-model fixtures, invalid-source diagnostics, formatter output, parts/markup, matchers, plural categories, and source/target compatibility cases. The parts fixtures now cover text, expression output, markup boundaries, expression/markup metadata preservation, and fallback parts across Rust/Swift/Python/Java/Go/PHP. Unicode MessageFormat WG tests are vendored under `mf2/third_party/message-format-wg/test`; the Rust, Java, JavaScript, Go, and PHP runners currently wire syntax success/error, bidi syntax, data-model error, `:string`, `:number`, `:percent`, and `:currency` functions, validation-only date/time/datetime built-ins, `:offset`, `:integer`, `u:` options, fallback, and pattern-selection checks with explicit skip/not-wired counts. Unquoted/escaped quoted literal placeholders, quoted variant keys, spaced tail options/attributes, markup options, `.match` adjacency syntax errors, strict simple/unquoted/attribute syntax errors, bidi syntax padding, local declaration dependency validation, NFC `:string` selection, unresolved-variable fallback formatting, selector function hooks, ranked selector best-match, selector-order priority, exact numeric/category row-order ties, fixed numeric keys for `:integer` and `:offset` locals, `:offset` plural-category selection on shifted values with literal and variable deltas, offset selector-priority ties, resolved-value metadata propagation through inputs/locals, strict `:number` operand/minimum-fraction handling, `:percent` formatting/selection semantics, validation-backed `:currency` formatting/non-selector semantics, and `u:dir` bidi direction overrides are now fixture-backed and move the official baseline to 461 passed, 0 skipped, 0 not wired in all five wired languages.
- MF2-03 — Grow the Rust, Java, JavaScript, Go, and PHP MF2 prototypes into clean-room tooling cores: keep parser, official model, runtime formatter, CLDR data, and diagnostics as separate modules while Rust expands toward CLI compiler and Wasm/LSP editor engine. Java now keeps conformance/demo/benchmark tooling under test sources instead of the production jar, passes raw host values through the function call boundary for typed date/time demos, and supports formatter/selector callbacks with inherited source metadata for official pattern-selection semantics. JavaScript now has a zero-dependency ESM parser/runtime with generated CLDR plural rules, source-to-model conformance, format/parts/fallback runners, the official Unicode suite, a catalog demo, plural-vs-Intl perf smoke, and a thin React wrapper demo. Go now has a native parser/runtime, generated all-CLDR plural rules, shared conformance tests, official Unicode tests, and a demo; it uses `golang.org/x/text/unicode/norm` for NFC until a generated zero-dependency normalizer is justified. PHP now has a native parser/runtime, generated all-CLDR plural rules, shared conformance runner, official Unicode suite runner, catalog demo, and simple perf smoke; it uses PHP `intl` for NFC. Rust/Swift/Python/Java/JavaScript/Go/PHP formatter callbacks now all expose rendered string values plus raw runtime operands where those languages have callback registries, with the shared catalog demo checking number-vs-string preservation; remaining package work is splitting parser/runtime/model/CLDR into proper publishable artifacts once the API boundary settles. Keep it fixture-driven and do not copy GPL `mf2-tools` code without license review.
- MF2-04 — Design native zero-dependency Swift MF2 targets: keep `MessageFormat2Runtime` parser-free with separate model, formatter, plural rules, and errors; add optional `MessageFormat2Parser` and a build-time compiler plugin so apps can ship compiled messages without a parser. Swift now exposes raw `MF2Value` on `MF2FunctionCall` alongside the rendered string; production date/time/number formatting still needs a native backend decision.
- MF2-05 — Design the Python MF2 runtime around the same conformance suite: keep formatter, plural rules, errors, and future parser/compiler boundaries dry; add source parser, official data-model loader, `format_to_parts`, diagnostics, and pytest runner. Python custom functions now receive both rendered string values and raw Python argument values. Babel remains adjacent infrastructure, not the MF2 runtime.
- MF2-06 — Add Mojito MF2 editor UI: React editor, TypeScript adapter, diagnostics/highlighting/completion/formatting/quick-fixes, locale-aware preview, target-language variants, and deterministic checks before AI assistance. The current prototype keeps one production-shaped React component demo at `mf2/editor-prototype/review-demo.html` and one low-level lab at `mf2/editor-prototype/lib-lab.html`. The shared `Mf2TranslationEditor` is backed by the public `@mojito-mf2/core` package, uses a ProseMirror rich adapter for placeholder atom nodes and paste-safe editing, keeps CodeMirror as a lazy raw-mode adapter, and exposes controlled target/locale/mode props for future Mojito embedding. It covers source-constrained `$`/`{` placeholder completion, missing-placeholder restore, source/target selector and variant diagnostics, markup shape/prop checks, target-locale CLDR row helpers, overlap diagnostics, bidi/parts previews, read-only embeds, IME-safe rich editing, raw undo/redo-preserving helper actions, and smoke tests for component/server-render behavior. The lab also carries a no-library raw textarea prototype beside CodeMirror for dependency evaluation. Next step is packaging the editor boundary and porting it only after Mojito has explicit message-format metadata and a deliberate CodeMirror/ProseMirror dependency decision. See `dev-docs/design/016-mf2-editor-mojito-workbench-port.md`.
  `mf2/react` now provides a thin FormatJS-style wrapper over the native JavaScript core with string hooks, structured parts/bidi inspection, rich-text component delegation, provider-level custom function and rich-component registries, parser-free compiled model catalogs through `@mojito-mf2/react/runtime`, source/target contract helpers, source-constrained insertion helpers, target-locale row actions, source/target scenario diagnostics, and a Vite demo plus server-render perf smoke.
  `@mojito-mf2/core` exposes parser/runtime/plural-rules/locale/errors subpaths with a package-boundary smoke test, giving React, editor, and reference benchmark code public imports instead of source-file reach-ins.
- MF2-07 — Build reference comparison and profiling into the MF2 program: ICU4J, ICU4C++, JS, parser throughput, and future official fixtures should run against the shared corpus with warmup-aware perf and memory reporting. Keep diagnostic fixtures such as the current simple-currency-vs-ICU4J cases separate from pass/fail runtime conformance until production backends exist. Treat the generated `experimental-number` CLDR data as droppable exploration until ICU comparison and runtime-backend decisions are made. Relative-time CLDR field data now has an all-locale generator/validated JSON artifact, file-backed CLDR sample fixtures, and a spec-quality `:relativeTime` duration-seconds draft with executable proposed runtime fixtures; the provider API, actual runtime function, and language-specific generated providers remain open.
- MF2-07a — Carry the JavaScript simple-message perf lesson across runtimes deliberately: add tiny compiled string-only paths for literal/plain-placeholder messages only where measurements justify them, keep them isolated from `formatToParts` and selector/function semantics, and record both speed and retained-memory tradeoffs. Avoid broad compiler abstractions until the same pattern repeats cleanly in Rust/Java/Swift/Python.
- MF2-08 — Promote CLDR plural generation into the runtimes: generated all-locale cardinal/ordinal rules are wired into Rust/Swift/Python/Java/JavaScript/Go/PHP and compared against ICU4J plural categories where a reference harness exists. Locale allowlists remain a generator option for future embedded/custom builds, not checked-in runtime data.
- MF2-09 — Cut an MF2 v0 around dumb placeholders plus CLDR plural selection: annotated selectors, multi-selector matches, cardinal/ordinal/exact `:number` and `:integer` selection fixtures, generated plural rules wired into Rust/Swift/Python/Java/JavaScript/Go/PHP, package-size checks, and reference comparison. Defer custom functions, compact IR, and grammar-contract workflows until after this subset is stable.
- MF2-10 — Extend the shared MF2 locale layer beyond the current tiny BCP47-first string helpers: add only the aliases, available-locale matching, and catalog fallback policy that runtimes actually need. Plural selection uses generated BCP47 keys, structural fallback, and an empty-but-supported plural-specific parent map; it must not depend on a locale object or general CLDR resource parents.
- MF2-11 — Keep MF2 runtime formatting preservation-first: `format()` must not silently normalize template literals, identifiers, argument strings, or formatted output. The `:string` selector path may compute NFC comparison keys internally for spec-compliant selection and duplicate-variant checks, but it must not mutate the model or returned strings. Keep other normalization/canonical-equivalence checks as explicit validation or tooling workflows, and grow decomposed-character fixtures in conformance to catch accidental normalization.
- MF2-12 — Continue full MF2 grammar coverage after the v0 slice: deeper identifier edge cases beyond the current Unicode/bidi/combining-mark fixtures, richer declaration/option/attribute grammar beyond the current quoted-tail tokenizer and duplicate option/attribute-name parser checks, function registry semantics beyond the current simple registry and demo registration, and deeper official data-model validation beyond the current declaration/variant/fallback/missing-selector-annotation/pattern/markup checks. Bidi isolation now has an opt-in shared fixture and runtime support across Rust/Swift/Python/Java; remaining bidi hardening includes value-direction metadata for filenames, URLs, identifiers, and other code-like formatter outputs where first-strong-character auto direction is not enough.
