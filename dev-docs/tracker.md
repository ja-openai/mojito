# Product & Tech Tracker

Purpose

- Capture improvement ideas and technical debt so we can prioritize intentionally instead of chasing every idea immediately.
- Treat this as a lightweight backlog: write down the idea, rationale, and any quick notes. Pull items into workstreams as capacity allows.

How to use

- Add new entries to the bottom with a unique identifier (e.g., `FRONTEND-01`).
- Keep notes concise: what problem it solves, rough scope/impact, and dependencies.
- When an item becomes active, move it to a dedicated design doc (see `dev-docs/design/`) or ticket system.

Backlog

- DEV-02 — Replace `npm install` with `npm ci` in Maven plugin executions for reproducible builds.
- DEV-03 — Plan the Spring Boot 4 migration before Spring Boot 3.5 OSS support ends; 3.5 keeps the app on Spring Framework 6 while security patching the current 3.x line.
- AUTH-01 — Figure out OSS-friendly auth story for static assets + APIs without Cloudflare: likely a tiny reverse proxy that terminates auth and sets a secure cookie for root-path SPA routes; dev can mimic edge by injecting `CF-Access-Jwt-Assertion`/`Authorization` headers via Vite proxy; document requirements (covers assets, avoids CSRF if cookies used) so we don’t restart from scratch.
- CI-01 — Add GH Action snippet to run `mvn -pl webapp -Pfrontend test-compile -DskipTests` (or `frontend:npm …check`) so frontend check uses the Maven-managed Node/npm.
- DOC-01 — Refresh published install/contributor docs for the current Spring Boot 3, JDK 21, MySQL 8, Maven-managed Node/npm, `webapp/frontend`, and root-path SPA workflow. Replace old `npm run start-dev` guidance with current Maven/Spring Boot and Vite commands.
- DOC-02 — Refresh auth/config docs after the embedded LDAP removal: document external LDAP URL-based setup, remove stale embedded-LDIF assumptions, and fix Spring Security 5/Spring Boot 2 wording in Spring Boot 3 docs.
- DOC-03 — Decide the docs-site tech refresh: remove or replace checked-in Bower dependencies and stale Gitter/Travis/OpenSource Box badges without breaking the static GitHub Pages build.
- FRONTEND-02a — Build container: React Query fetch/search/filter, selection state, scroll-to-selected, keyboard nav, and tanstack virtualization hooks (estimate/measure) without UI markup.
- FRONTEND-02b — Implement pure view: virtualized repos table with sticky header and inline search, selection highlight, metric link callbacks, and locales pane layout that mirrors the prototype.
- FRONTEND-02c — Locale data & accessibility: map selected repo to localeDetails or call `onLoadLocales(repoId)`, handle loading/error/empty states, add scope/aria labels and focusable selected row, and keep CSS state classes minimal.
- REVIEW-01 — Evolve review automation beyond the MVP executor: add business-day due-date handling, optional team/vendor delivery pools, and final claim strategy on top of the existing feature-based project creation, open-project exclusion, and optional translator auto-assignment.
- REVIEW-03 — Revisit review automation cron startup/Quartz lifecycle. Current persisted trigger path can instantiate `ReviewAutomationCronJob` too early during app startup; design a proper fix that matches existing Quartz patterns without changing global scheduler startup behavior opportunistically. The decision-integrity canary uses static Quartz beans and deliberately does not change or depend on this dynamic startup path.
- REVIEW-04 — Use the new review-project latency probes to choose the next save-path optimization from production/staging data. Compare `initialRead`, `currentVariantWrite`, `decisionWrite`, `decidedCountUpdate`, `detailReload`, standalone integrity checks, glossary matching, and AI-review precompute hit rate before adding more caching or background work. See `dev-docs/design/021-review-project-latency.md`.
- AI-REVIEW-03 — Verify optimistic capacity admission under concurrent interactive review: distinguish per-user rejections and global occupancy warnings from reservation conflicts and best-effort admissions, and measure delayed release/expiry. Start with a generous approximate limit of 12 per user and a warning-only threshold of 800 across the cluster; use the content-free logs to investigate saturation before considering tighter admission controls. Measure successful latency and timeouts by preset to tune the new speed-specific total budgets, including larger inputs. Measure HTTP/database waiting separately from task-created-to-provider time; preserve full context and reasoning quality, and verify completed browser reviews. See `dev-docs/design/010-ai-observability.md`.
- AI-REVIEW-05 — Validate submission-burst observations before enforcing any rate policy. Start with 3 requests/second and a burst allowance of 10 per authenticated user, shared across pods; warnings never reject work. Inspect incomplete observations, clock skew, CAS contention and datasource headroom as well as excess events. Measure automatic-review latency and unnecessary provider calls after the cancellable 300 ms navigation delay, retaining immediate manual requests. See `dev-docs/design/010-ai-observability.md`.
- REVIEW-05 — Decide whether Review Project find/replace local undo/redo checkpoints need durable backend storage before broad use. The current `/review-projects/{projectId}/find-replace` slice stages changed working targets in `review_project_text_unit_suggestion` without writing TM, or explicitly accepts and decides changed rows through the normal Review Project save path. See `dev-docs/design/020-review-project-find-replace.md`.
- REVIEW-06 — Operationalize exact repeated-current-target discovery: validate the temporary-CTE whole-database and Review-Project plans, timeout/resource behavior, and existing audit-index probe under production-equivalent staging load, then paginate one complete restart-validated scan and connect semantic review to the resulting candidate clusters. Do not add a permanent target-hash index to `tm_text_unit_variant` for this rare audit. Keep `review_project.audit_decision_integrity` only for recent structural checks and timing telemetry; its adjacency heuristic is not the carryover inventory. See `dev-docs/design/027-review-project-decision-integrity-audit.md` and `dev-docs/design/030-repeated-current-target-discovery.md`.
- REVIEW-07 — Roll out and verify the source-only content-free Review Project decision attribution: executing bundle identifier, page/operation/request sequence, retained draft/suggestion ownership, and post-commit outcome logs. Confirm bounded logs and retention/access, fresh loads versus long-open tabs, and explicit conflict recovery in the deployed client/server. Client claims do not identify human versus CUA, and historical carryover causality remains unproven. See `dev-docs/design/031-review-project-editing-state.md` and `dev-docs/investigations/2026-09-01-review-project-carryover.md`.
- REVIEW-08 — Release and verify the Review Project editing-state changes: deploy matching client/server artifacts, refresh existing translator tabs, check actual OS input methods and vendor roles/SSO, and observe save/conflict behavior under realistic concurrency. Decide whether draft recovery must survive crashes or confirmed reloads; current retention is in memory. Local evidence and remaining limits are in `dev-docs/investigations/2026-09-02-review-project-confidence-verification.md` and `dev-docs/design/031-review-project-editing-state.md`.
- WORKBENCH-03 — Decide whether `/api/textunitsBatch` should be admin-only end-to-end. The new workbench import UI is admin-gated, but backend auth still follows the broader `/api/textunits/**` permissions.
- WORKBENCH-04 — Revisit workbench import round-trip semantics for `status` and `includedInLocalizedFile`. The current batch import backend ignores those fields even though export/import can carry them.
- PLATFORM-01 — Operationalize and then harden the `webapp` MCP bad-translation workflow: deploy the guarded `translation.apply_corrections` operation, verify automatic tool registration, pre-conversion admin denial, bounded-payload rejection, payload-free variant-write logs, a stale no-write canary, and one separately authorized staging read-back; then improve review-project lineage confidence, add better operator audit visibility, and decide whether sync-only `/api/mcp` is enough before adding SSE/session support. See `dev-docs/design/011-webapp-mcp-bad-translation-scaffold.md` and `dev-docs/design/027-review-project-decision-integrity-audit.md`.
- PLATFORM-02 — Extend task inspection beyond single-id lookup if operators need broader triage: recent failed-task search, repository-scoped task history, and retention expectations for pollable-task input/output blobs.
- AI-TRANSLATE-01 — Reassess whether source prompt rules should also split legacy batch AI Translate files after the no-batch implementation has production usage. See `dev-docs/design/017-ai-translate-source-prompt-rules.md`.
- AI-TRANSLATE-02 — Build controlled, review-grounded translation comparisons before claiming linguistic improvement: freeze source/context and complete configuration versions, prevent accepted-target leakage during replay, and run explicit integrity checks without TM import. Compare models, prompts, glossary, and context using paired datasets, calibrated quality measures, blinded review, and scoped promotion/rollback. Measure the ICU/MF2 candidate validator and one-attempt repair policy against human-reviewed cases: cardinal/ordinal coverage, exact-number and selector contexts, legitimate count omissions, false warnings, repair success, latency, and cost. Add bounded optimization after evaluator calibration. Proposed architecture and implementation sequence: `dev-docs/design/033-translation-evaluation-and-learning.md`; current behavior remains documented in `029-ai-translation-feedback-evaluation.md` and `032-ai-translation-quality.md`.
- AI-TRANSLATE-03 — Before enabling the default-off `l10n.ai-translate.message-format-validation-enabled` policy, fix false rejections of valid selector branches and gaps in formatter validation with regression tests. Consolidate shared read-only integrity checks separately from generation requirements such as complete plural coverage. Reuse existing attempt/lineage tracking for online and Batch repairs, with distinct provider retry, candidate repair, and import failure outcomes and budgets. See `dev-docs/design/032-ai-translation-quality.md`.
- AI-REVIEW-01 — Make precomputed reviews safe for the selected preset and review style before re-enabling them in Review Project or text-unit details. These pages currently bypass the legacy cache because rows lack model/settings provenance. Add configuration and prompt/glossary/page-context freshness checks beyond `for-frontend-v2`, then choose a scoped PM/admin/scheduled trigger, retry policy, and hit-rate threshold without warming the cache from translator page loads. Use the new request-usage metadata for adoption/latency, and human-reviewed comparisons for quality. See `dev-docs/design/010-ai-observability.md`.
- AI-REVIEW-02 — Validate neutral NBSP/NNBSP context on human-reviewed locale examples, including correct typography and actual spacing defects; measure additional live-review latency for narrow-NBSP-only targets. Design neutral UI presentation without losing the inspection signal used by plain-editor linguists or duplicating Hidden chars controls. Any deterministic typography warnings need approved locale/style rules. See `dev-docs/design/032-ai-translation-quality.md`.
- GLOSSARY-03 — Add semantic glossary retrieval on top of the current lexical/span matching so MT and AI review can retrieve consistency terms even when the exact source form differs, and distinguish ambiguous short product names from ordinary words within longer phrases. Inline-tag normalization preserves lexical matches but does not resolve this ambiguity. The current architecture keeps room for this via `MatchedGlossaryTerm`, AI-assisted extraction metadata, and per-term evidence.
- GLOSSARY-04 — Add configurable extraction noise controls and approval policies for AI-assisted glossary mining. The current extractor still uses a static stop-word list plus AI reranking; we likely want glossary-level/global ignore words, repository-specific extraction presets, and confidence thresholds.
- GLOSSARY-05 — Harden glossary candidate-term review beyond the first pass: cluster duplicate source-term proposals, add richer repository/review context for reviewers, and decide how much of the remaining legacy translation-proposal model should survive versus being retired entirely.
- GLOSSARY-06 — Add readiness summary, diff/audit views with original-term attribution, subsequent editor tracking and replacement history, and proposal-based feedback to the active glossary workspace. The editor shows the existing creator of the current source version; durable history remains separate from this attribution and editable term-level decision notes. Existing readiness/proposal APIs still need a supported UI. See `dev-docs/design/011-glossary-workspace.md`.
- GLOSSARY-08 — Finish glossary MCP client integration: validate MCP client auth/config against `/api/mcp`, decide whether an authenticated local bridge or MCP-specific token is needed, and decide whether TM/source-glossary translation suggestions should get a reviewed write workflow beyond `bulk_upsert`. The core tools now cover glossary create/update, term list/search/review-plan/bulk-upsert/reference-link, term-index candidate seeding, glossary-to-candidate seeding, existing-term candidate linking, image upload for screenshot evidence, and TM translation suggestion. See `dev-docs/design/012-glossary-mcp.md`.
- GLOSSARY-10 — Decide the operational refresh policy for the raw term index: scheduled cadence, repository scope, retry behavior, and retention/pruning for `term_index_refresh_run` rows once automatic refreshes exist.
- GLOSSARY-11 — Harden raw term-index curation after the first glossary workspace pass: add structured metadata rendering for seeded candidates, manual split/merge review controls, duplicate/alias clustering, and richer filters for ignored/accepted suggestions. See `dev-docs/design/013-glossary-raw-term-index.md`.
- GLOSSARY-14 — Add the next layer of candidate-queue sort/filter maturity now that `/settings/system/glossary-term-index/candidates` is backed by `term_index_candidate`: confidence sorting, created/updated date filters, source-type filters, and clearer distinction between candidate confidence, review confidence, and extracted-term occurrence confidence.
- GLOSSARY-15 — Finish source terminology review resolution: terminology review projects now cover glossary-backed terms and generated term candidates with specialist-input and PM-resolution phases. Candidate review projects defer glossary term creation until PM acceptance; remaining work is AI glossary assignment, cross-vendor/teamless review workflow decisions, and richer owner-side proposal edits.
- GLOSSARY-16 — Evaluate best-effort glossary-and-locale pub/sub invalidation only if the existing 30-second watermark-backed cache reconciliation is too stale; keep TTL/watermarks as the recovery path and do not introduce shared-row counters or write-path locks.
- TM-01 — Monitor the `TextUnitSearcher` Hibernate Criteria rollout. The Criteria implementation is now the main `TextUnitSearcher`; the old native query builder, native query dependency, shadow comparator, and replacement package are removed. Remaining work is production SQL/load monitoring and any targeted tuning, especially around aggregated usages ordering/separators. HQL/native-SQL alternatives are documented but no longer kept in code. See `dev-docs/design/014-text-unit-searcher-replacement.md`.
- DB-01 — Continue native SQL portability cleanup after the first low-risk pass. Remaining native paths are documented in `dev-docs/design/015-native-sql-portability.md`; prioritize exact equivalence tests for business-critical joins/aggregates, then duplicate-key/concurrency tests for cache and term-index insert-if-absent behavior.
- DB-02 — Validate prefix-scoped `mblob` cleanup under production load, tune batch size and pause settings, and decide whether the remaining generic expiration scanner can be retired after configured policies cover all temporary blob prefixes.
- BLOB-01 — Harden Azure Blob Storage rollout after the admin monitoring, production-equivalent tagged write/read/delete probe, and opt-in image migration job: verify workload-identity permissions include blob index-tag writes, update deployment/local configuration from deprecated `l10n.blob-storage.type` to `l10n.blob-storage.default-type`, validate a real Azure account/container in integration tests, and configure provider lifecycle rules for temporary blobs. See `dev-docs/design/018-azure-blob-storage.md`.
- BLOB-02 — Operationalize durable bulk-import provenance after the admin history/report view: configure and verify the explicit production Azure route for `bulk-import-lineage`, initially with database fallback so historical payloads remain readable; verify/backfill required objects before switching to plain Azure; define permanent retention/access policy; and monitor missing or failed run payloads. Per-string evidence is stored through the configured blob route rather than a relational item table; existing translation comments carry a run-ID breadcrumb when available. See `dev-docs/design/018-azure-blob-storage.md`.
- SEARCH-01 — Harden the opt-in locale-aware OpenSearch text-unit index after the local provisioning, language-routed source/target analyzers with ICU/Kuromoji/Nori/Smart Chinese plugins, admin bootstrap/reindex, and fuzzy-search slice: choose managed staging/production infrastructure and authentication, define incremental update/delete propagation and index rebuild ownership, and design embedding generation before enabling semantic retrieval. See `dev-docs/design/022-search-index.md`.
- SEARCH-02 — Evaluate the opt-in Search preview in Review Project and text-unit details before enabling it by default: measure searches across repositories/locales and persist upstream translator provenance on variants if results need author attribution beyond the account that saved them. See `dev-docs/design/034-review-project-search.md`.
- SOURCE-01 — Harden JSON config localization after the v0 DB-backed setup, split CLI pull/export/push commands, and backend Statsig provider adapter: add bundled review-project creation, wire screenshot attachments into the Workbench/review-project screenshot path, add repository-page entry points for configured JSON setups without affecting repository-list performance, and add source/schema validation for generated output. See `dev-docs/design/019-json-config-localization.md`.
- SOURCE-02 — Harden string authoring after the branch-convention MVP: reconcile temporary `authoring/` branches against `master`, decide whether typed branch metadata is needed beyond the naming convention plus cleanup date, and extract more shared editor components with JSON config localization if the editors keep converging. See `dev-docs/design/020-string-authoring.md`.
- SOURCE-03 — Continue portable Java/Rust localization conversion for Android, Apple, gettext, properties, JSON, CSV/Magento, RESX/RESW, XTB, JavaScript/TypeScript, HTML, and YAML; preserve native-verified extraction, source templates, protected content, comments, plural rules, configured Mojito workflows, and shared conformance fixtures. See `dev-docs/design/024-portable-localization-file-formats.md`, `025-portable-localization-compatibility-ledger.md`, and `026-portable-localization-custom-filter-parity.md`.
  - Keep `push`/`pull`/localized-asset `import`/client-side `extract` defaulting to Okapi while supporting explicit `--converter portable`; preserve existing filter options, reject unsupported formats, and honor explicit `--converter okapi` even when the backend-wide portable property is enabled.
  - Maintain backend extraction/output and CLI-local extraction/diff/check tests, including stable properties/Apple/YAML identities, correctly decoded JSON descriptions, owner comments/usages, selector-owned Russian Foundation plurals, locale-bounded plural import, empty-target integrity handling, stored extracted-content bypass coverage, HTML/YAML/JavaScript runtime-safety oracles, existing per-format CLI regression datasets, representative large-catalog locale round trips, and native platform verification.
  - Keep `.xcstrings` backend selection gated while its standalone target-insertion API requires an atomic complete ICU target that cannot yet be assembled safely from Mojito's per-category TM units.
  - Maintain the explicit `push --converter portable --migrate-legacy-json-comments` path for JSON descriptions whose corrected escaping changes translation-memory identities; preserve approval only for unambiguous, asset-local, name/source/escaped-comment matches without changing shared leveraging policy.
  - Before enabling portable conversion globally, resolve or explicitly exclude legacy tests requiring platform-invalid Android/Apple/gettext inputs, unsupported Android escaping, protected plural extraction bugs, duplicate properties, or exact Okapi XML/YAML reserialization.
  - Decide whether conflicting duplicate Java properties need separately owned canonical identities before claiming complete legacy filter parity.
  - Keep the portable shadow observer disabled by default until production telemetry, alerting, and cutover rollback ownership are defined.
- SOURCE-04 — Finish Apple `.stringsdict` cutover: preserve XML/binary plist ownership, independent plural/device/width branches, protected entries, byte-preserving source templates, and safe binary target-only category expansion.
- SOURCE-05 — Finish gettext enrollment: preserve plural formulas, metadata/comments, contexts, encodings, untranslated policies, and native `msgfmt` round trips.
- SOURCE-06 — Extend durable, source-preserving skeleton transport and rollout coverage for every supported native format; keep bilingual XLIFF deferred until its segment, inline-code, target-state, and skeleton contracts are specified.
- SOURCE-07 — Extend source-less pull beyond JSON root-map catalogs, or add remote-only asset discovery, only when a concrete consumer can define the required output structure and path mapping. See `dev-docs/design/023-pull-with-no-source.md`.
- SOURCE-08 — Roll out the native Rust `push`/`pull`/localized-asset `import` CLI one localization pipeline at a time: wire its locked tests and Clippy into CI, compare generated files and HTTP payloads with Java, validate real Cloudflare/header, form-login, bearer, and client-credentials authentication, and define ownership for packaging and rollback. Keep interactive MSAL and bilingual XLIFF on the Java CLI until those contracts are explicitly implemented. See `dev-docs/design/028-native-rust-localization-cli.md`.
- SOURCE-SCRIPT-01 — Expand Xcode locale canonicalization only when original fixtures and native compiler/runtime evidence prove alias, script, region, and fallback behavior.
- EDITOR-01 — Connect the generic protected text editor to backend integrity-checker placeholder ranges. The frontend currently has instant ICU/html extraction, common Android/iOS/printf placeholder extraction, and an MF2-like demo token mode; production MF2 rows use the dedicated structured editor. Repository-specific placeholders should come from a REST endpoint backed by configured integrity checkers so Workbench/review/detail can protect markdown, URL, Python f-string, and other project-specific spans consistently. See `dev-docs/design/016-protected-text-editor.md`.
- EDITOR-02 — Deploy and verify the Characters menu clipping fix in Review Projects and inline Workbench: confirm Word joiner insertion, caret/focus preservation, and keyboard access in the live UI. See `dev-docs/design/016-protected-text-editor.md`.
- MF2-01 — Finish a shared configurable budget API for source/model size, declarations, selectors, variants, output and diagnostics. Parser progress, bounded numeric precision/expansion, Go padding, literal-history memoization and deadline-guarded mutation regressions now cover the confirmed failures. Variable-dependent histories remain a complexity limit; browser and server input guards remain application policy. Specify byte/character accounting and recovery before broad untrusted-catalog use. See `mf2/spec/runtime-limits.md`.
- MF2-02 — Decide whether to expand the documented parts API and platform capabilities: all eight direct official bridges now evaluate every assertion with exact maintained non-pass dispositions, and all seven platform registries run the 47-case adapter corpus. Go still has no platform registry; Rust ICU4X lacks currency/relative time; parts retain model metadata rather than exposing the full upstream resolved-parts shape. See `mf2/conformance/coverage-audit.md`.
- MF2-03 — Roll out the opt-in repository/extension `MF2` integrity checker after locale-aware preflight, then make configured classification consistent across every text-unit/review API, including mixed-format assets. Server save, review, guarded correction and import paths now supply authoritative target locale; legacy checks without locale remain structural. Audit intentional PM/admin/import/AI bypasses separately and do not infer activation or deployment from source wiring. See `dev-docs/design/015-mf2-foundation-roadmap.md`.
- MF2-04 — Finish the translator-facing guided editor without weakening the structured model: add an explicit focused expansion only if complex selector/repair work needs it, explain locale plural categories with examples, add explicit locale-aware form add/remove controls, and provide scoped active-pattern/placeholder repair separately from full-document **Edit placeholders** mode. See `dev-docs/design/016-protected-text-editor.md`.
- INTEGRITY-01 — Roll out the opt-in `FORMATJS`, `DOLLAR_TEMPLATE`, and `MF2` repository checkers one repository and extension at a time. Before activation, run the bounded read-only CLI preflight (with target locale for MF2), resolve target findings, record source defects separately, and preserve unrelated checker rows because `repo-update -it` replaces the complete set. Map each downstream check to conformance cases before retirement, define rollback ownership, and audit intentional bypasses such as leveraging, backup restore, rollback, PM/admin Workbench saves and review decisions, and pseudolocalization independently. Keep waivers, external-review routing, automatic repair, and remaining `extended` cases outside the save gate until separately approved.
- INTEGRITY-02 — Correct literal-percentage false positives in `PRINTF_LIKE` (for example, `100% so` matching `% s`) while preserving valid printf checks, and display readable server validation errors in Workbench.
- SETTINGS-01 — Verify the V108 `user_preferences` schema upgrade and account save/readback in staging before release. Disposable MySQL migration/concurrency checks pass; deploy the backend endpoint and schema with the frontend and verify the running instance. See `dev-docs/design/035-account-preferences.md`.
- AI-REVIEW-04 — Roll out the staged agent-review workflow with matching V111/V112 migrations, backend/MCP, frontend, and the repository Codex skill. Validate a shared-run takeover and a human-reviewed multilingual pilot, including sampled unflagged strings, unnecessary/incorrect suggestions, missed errors, and reviewer effort. Core runs/proposals/feedback, typed incident routing, and one-human diff review are implemented. General incident routing, independent review groups, and reconciliation remain outside v0. See `dev-docs/design/036-agent-translation-review.md`.

- SLACK-01 — Verify the shared compact notification format with a real request, attachment links, and reassignment in its saved thread after deployment. No schema migration is needed for this change. Inline screenshot previews and adopting handoff threads from another Slack workflow remain separate integrations. See `dev-docs/design/007-review-automation-foundation.md`.

- QUEUE-01 - Finish release validation of the disabled queue foundation integrated from master `ed4bc31eda` and queue `086f26b16c`; keep production adoption distinct. Queue-only migrations now use V113, preserving existing migration bytes. Native MySQL Boot fresh-install and populated V112 upgrade rehearsals pass; complete the required hosted database matrix and verify each target's applied history and timestamp/legacy-lineage provenance. Routing flags do not hide Flyway or shared Quartz task/blob/generation changes. Future AI Review support, complete Quartz removal and OSS publication are not prerequisites for that narrower landing.
  Follow the [deployment and canary runbook](design/async-job-queue-canary-runbook.md): first verify a disabled release, then gate a stable direct-only cohort before sequential platform tests. Config rollback requires same-image pod restarts, stops producers before draining accepted work, and cannot undo DDL or business effects. If execution risks corruption, contain affected consumers before considering a drain; startup flags cannot fence already-running business writes. Tracked and parallel pipeline runs that remain on Quartz are compatibility evidence, not queue coverage; resolve the stated activation prerequisites before enabling.
  Preserve the [isolated Flyway artifact contracts](design/async-job-queue-review.md#fresh-flyway-artifact-contract-2026-09-13-utc) for both databases and the [MySQL application chain](design/async-job-queue-review.md#mysql-application-flyway-chain-2026-09-13-utc). The integrated chain now contains 112 migrations (110 SQL, two Java), ending at queue V113; the 108-migration results remain historical evidence from the older base. CI requires fresh and populated V112-to-V113 Boot migration/restart tests on both MySQL 8.0 and 8.4, with actual server-version assertions and no required skips or reruns. Require a passing expanded hosted lane for the release revision and resolve each target's migration history plus Flyway 11.7.2's MySQL tested-version warning before database adoption.
  Fix the [foundation history dependency](design/async-job-queue-review.md#foundation-history-dependency-2026-09-12-utc): the original admin commit references repair classes from the following adapter commit. Generic-admin and repair configuration/HTTP fixtures are now separated in the current tree; place them in their respective chunks when restacking, retain later core fixes, and verify each resulting commit boundary. Current HEAD tests do not certify the old stack; do not cherry-pick its first two commits as an independently buildable foundation.
  Keep assetlocalize and its separate fan-out control default-off until the finite gates in [Current Readiness](design/async-job-queue-review.md#current-readiness) pass. Implement durable direct admission first: settle the [API/authorization/lifetime decisions](design/async-job-queue-admission.md#decisions-versus-defaults), then persisted reservations, verified pinned input, atomic JPA/JDBC task-plus-queue acceptance and same-key unknown-commit recovery. Reject ambient transactions at orchestration entry before uploads or writes; preserve the distinct public independent-enqueue and internal enlisted-enqueue contracts. Pure identity/decoding models and enlisted INSERT tests are prerequisites, not recovery. Missing applied history blocks choosing DDL, not independent core fixes or tests.
  Build parent recovery on accepted direct requests: approve duplicate-tag/size policy, persist frozen manifests and stable child slots, and reconcile partial parents without duplicating accepted children. Preserving local mappings/original errors despite diagnostic failures is not durable reconstruction. Keep the non-null pullRunName exclusion until the [lineage authority contract](design/async-job-queue-lineage.md#exact-scope-and-owner-decisions), business-generation fencing, reader compatibility and old-writer drain are implemented. Queue-row leases and read-time terminal-task checks do not fence business writes.
  Agree input/result/repair/replay horizons and implement reference-aware cleanup across database/cloud/fallback copies. Prove accepted input and unresolved winning-output publication survive cleanup; permanent writes and database retention fixes are not queue pins. Finish stable canary route selection, immutable route/identity handling, legacy PollableTask/HTTP/CLI parity and bounded rollback/drain rehearsal before enrollment. Shadows need immutable dependencies or isolated stores, not only private output keys. Retain Quartz cron/orchestration; statistics needs its own keyed coalescing, generation fencing and notification-deduplication work. Use the [migration plan](design/async-job-queue-quartz-migration.md) and [admission protocol](design/async-job-queue-admission.md) for acceptance matrices, preserved regressions and mixed-version constraints.
- QUEUE-06 - Agree on extraction and host wiring for one Spring-aware queue Maven module; the source-in-place [packaging probe](queue-library-probe/README.md) is not a published library. Preserve public execution/inspection, producer-only composition, optional wakeup and explicitly scheduled maintenance, plus independent-JAR provenance and JDBC/JPA transaction boundaries without Mojito bootstrap or AspectJ. Host datasources must supply independent physical connections; matching resource identities and known-single-connection rejection do not certify hidden sharing or pool sizing.
  Preserve the [native MySQL ordinary-JAR proof](design/async-job-queue-review.md#native-mysql-ordinary-jar-consumer-2026-09-12-utc) and [native PostgreSQL 16.15 consumer proof](design/async-job-queue-review.md#native-postgresql-ordinary-jar-consumer-2026-09-12-utc), including successful wakeup and caller commit/rollback isolation. The same two MySQL lean execution/maintenance contracts plus both boundary tests now also pass on [native 8.4.11](design/async-job-queue-review.md#native-mysql-84-compatibility-lanes-2026-09-12-utc), with build/cache JAR identity checked; refresh the full configured CI lanes before extraction approval. The [host-owned JPA suite](design/async-job-queue-review.md#independent-jar-jpa-database-lanes-2026-09-12-utc) now parameterizes its six contracts across HSQL and both opted-in databases; all six cases plus JAR-boundary checks pass natively on PostgreSQL 16.15 and [MySQL 8.0.43](design/async-job-queue-review.md#native-mysql-independent-jpa-host-2026-09-12-utc). The same eight checks now pass on [native MySQL 8.4.11](design/async-job-queue-review.md#native-mysql-84-jpa-transactions-2026-09-12-utc) with a freshly rebuilt JAR; the separate application fixture also passes its 35 JPA/JDBC methods there. CI still requires real-database opt-in with zero reruns; these macOS-native checks do not certify the full hosted/Linux lane. Public independent enqueue is not atomic host/queue admission.
  Keep Mojito admission, PollableTask, blobs, lineage and Quartz adapters outside the generic module. Settle artifact/API naming, schema ownership/adoption, supported-version compatibility, licensing/provenance and repeatable DB CI before release; follow the [finite extraction gates](design/async-job-queue-library.md#finite-extraction-gates). No large extraction or publication without agreement. Separately review the proposed [asynchronous handler extension](design/async-job-queue-asynchronous-handlers.md) only for a concrete durable workload: retain claim/capacity beyond thread return, handle deadline/cancellation/transport-settlement/shutdown races, and distinguish provider retries within an execution from queue replay. Retain direct AI Review dispatch while restart failure is acceptable; durable waiting/enrollment and hard capacity/rate limits need separate policy decisions. Master's warning/approximate accounting is not a hard semaphore. AI Review remains unenrolled; the proposed API is not implemented and is not a foundation-landing prerequisite.
- QUEUE-03 - Decide the external admin/replay surface and user-level audit requirements. Existing admin counts and payload-redacted inspection plus assetlocalize repair are implemented; raw row replay is not a supported task-recovery workflow. Require authorization, retained payloads, domain-aware fresh linked requests and durable replay receipts/unknown-outcome recovery before exposing automated replay. Finish automatic reconciliation of recorded terminal winners rather than rerunning business handlers.
  Preserve the [admin HTTP authorization regression](design/async-job-queue-review.md#admin-http-authorization-2026-09-12-utc): production request rules and real controllers deny anonymous/USER/TRANSLATOR/PM access before service invocation, while ADMIN reads redact payloads and allow repair only. Removing the queue matcher demonstrably breaks the role test. This mocked-service HTTP fixture does not verify production authentication providers, CSRF or staging access; those rollout checks remain required.
  Preserve [handler diagnostic containment](design/async-job-queue-review.md#handler-diagnostic-containment-2026-09-12-utc) and the [shared generation boundary](design/async-job-queue-review.md#shared-generation-diagnostic-containment-2026-09-13-utc): compound timer/logger failures cannot discard generated results or replace original generation/callback outcomes; nested JVM-fatal errors still propagate. The shared service affects Quartz too, even with queue routing disabled. Unit fault injection and HSQL compatibility controls do not establish durable callback delivery, business fencing or database-outage recovery.
  Audit the broader checked-business/global task-error disclosure contract and historical rows separately; the new queue diagnostic redaction does not sanitize legacy data or change access policy. Preserve original errors and acknowledged outcomes when logging, metrics or wakeups fail. Repair now isolates ordinary counter/logger failures and propagates nested JVM-fatal errors; [HSQL committed-finish and lost-ack regressions](design/async-job-queue-review.md#repair-diagnostic-isolation-2026-09-12-utc) preserve typed errors, HTTP redaction and unchanged already-finished rows. These are service-boundary faults, not network recovery or automatic reconciliation. Generic submission propagates nested JVM-fatal errors without retry or compensation, including after an acknowledged enqueue; that does not supply a caller retry identity or prove rollback. Verify exported admission histograms, per-pod/queue last-success freshness, missing-target alerts, client-observed completion and publication reconciliation using the [monitoring acceptance checklist](design/async-job-queue-canary-runbook.md#monitoring-acceptance). The optional metrics profile does not enable routing; local registry/fault tests are not live alert proof. Current behavior, regression evidence and remaining limits belong in the [review ledger](design/async-job-queue-review.md), not completion history in this backlog.
- QUEUE-04 - Staging-verify optional PostgreSQL LISTEN/NOTIFY hints, polling fallback, reconnect/session cleanup and alert wiring. MySQL retains jittered adaptive polling. The [native PostgreSQL 16.15 public-consumer run](design/async-job-queue-review.md#native-postgresql-ordinary-jar-consumer-2026-09-12-utc) now verifies successful hints without committing resumed caller writes, independent producer/consumer notification delivery and polling recovery after injected hint failure. It does not certify arbitrary datasource wrappers, actual network loss or staged failover. Wakeup connections must be independently leased. Preserve fatal read/cleanup error propagation; complete network/failover soak using the [wakeup composition contract](design/async-job-queue-library.md#optional-wakeup-composition).
  Preserve the two [native PostgreSQL pooled-listener contracts](design/async-job-queue-review.md#native-postgresql-listener-sessions-2026-09-12-utc): same-session reuse clears LISTEN and restores auto-commit, while real backend termination reconnects despite a failed diagnostic counter. These one-slot Hikari fixtures use a mock coordinator; they do not certify shared-pool sizing, network partitions, database failover or staged alert behavior.
  Preserve the [combined PostgreSQL worker/listener restart contract](design/async-job-queue-review.md#combined-worker-and-listener-restart-2026-09-12-utc): the same listener and real coordinator recover from an observed outage, deliver new hints and complete new jobs; after listener shutdown, polling alone completes another job. Retain exact row/callback, drained-capacity and pooled-session cleanup checks. Separate worker/listener pools do not establish shared-pool sizing, sustained soak, Docker SIGKILL, replica promotion, network-blackhole recovery or durable notifications; those rollout checks remain open.
  Preserve [listener recovery logging containment](design/async-job-queue-review.md#listener-recovery-logging-2026-09-12-utc): ordinary connection-failure/reconnect-sleep/stopped-thread log failures no longer terminate recovery or escape shutdown, while nested JVM-fatal errors propagate unchanged. Mocked-JDBC thread regressions and rebuilt ordinary-JAR controls do not replace real database/network failover or staging verification.
- QUEUE-05 - Complete sustained/shared-pool and target-workload validation while preserving the required MySQL 8.4/PostgreSQL 16 CI gate. The [hosted Linux/Docker job](https://github.com/ja-openai/mojito/actions/runs/34793526078/job/103822142084) passed on release `1be80c40cae35481c47fbb501e96ca4163e81022` on 2026-09-14 UTC, first attempt, with zero required skips or reruns. The [native MySQL 8.4.11 run](design/async-job-queue-review.md#native-mysql-84-store-contracts-2026-09-12-utc) now repeats the nine maintained store/identity/claim-lock groups on macOS ARM64, including all ten held-open backlog/batch scenarios. Keep this separate from the [8.4 JPA proofs](design/async-job-queue-review.md#native-mysql-84-jpa-transactions-2026-09-12-utc) and [six native 8.4 fault-recovery cases](design/async-job-queue-review.md#native-mysql-84-fault-recovery-2026-09-12-utc); the [native 8.4 compatibility lanes](design/async-job-queue-review.md#native-mysql-84-compatibility-lanes-2026-09-12-utc) now also pass 14 timezone cases in each UTC/Los Angeles JVM, one adapter method with 15 internal scenarios and four lean consumer/boundary checks. Preserve the forced claim index and account for old-worker broad locking, up to 2B candidate locks, and scans of nonexpired/case-equivalent rows. Preserve the [maintained retention lock regression](design/async-job-queue-review.md#bounded-mysql-retention-regression-2026-09-12-utc): all 20 DONE/FAILED, batch and backlog scenarios pass on native 8.4.11 with peer replay before purge commit and unchanged excluded rows. Preserve it in the required Docker job, where it now passes. Keep retained-lock bounds distinct from actual scanning/optimizer cost; extend the [single-scenario native retention plan](design/async-job-queue-review.md#native-mysql-retention-plan-2026-09-12-utc) to target versions and representative workloads. Preserve the [native UTC/timezone matrix](design/async-job-queue-review.md#native-mysql-timezone-contracts-2026-09-12-utc), JPA/JDBC contracts, claim/renewal/retry/completion uncertainty, session-loss and separate-JVM crash cases in the required CI selection, which now passes. New-write timezone checks do not establish historical timestamp provenance or a safe mixed-worker cutover.
  Preserve the [required-report gate](design/async-job-queue-review.md#required-database-report-gate-2026-09-13-utc) after each clean application/lean/JPA database lane. Required suites, per-database test-count floors, unexpected skips and retry artifacts are now checked; only the named opt-in throughput benchmark may skip. The exact-release hosted report gates accepted 199 application passes plus one optional benchmark skip, 18 ordinary-JAR consumer passes and 36 host-owned JPA passes, with no required skips or reruns. The application lane includes fresh-install and populated V112-to-V113 Boot migration/restart tests on MySQL 8.0 and 8.4. Keep matrix floors aligned when required contracts change; report validation alone does not prove database identity or capacity.
  Keep the [minute-boundary retention fixture](design/async-job-queue-review.md#retention-clock-fixture-2026-09-12-utc) deterministic: explicit SQL seconds fix a reproduced HSQL test flake, not production clock or historical timestamp provenance.
  Preserve the [lost TCP commit-reply contracts](design/async-job-queue-review.md#lost-tcp-commit-replies-2026-09-12-utc): real enqueue/completion socket timeouts retain committed rows and terminal DONE. The [claim extension](design/async-job-queue-review.md#runtime-lost-claim-recovery-2026-09-13-utc) prevents unacknowledged dispatch and reclaims only after natural expiry; the [heartbeat extension](design/async-job-queue-review.md#runtime-lost-heartbeat-recovery-2026-09-13-utc) preserves the live handler and renews/completes on its original token and attempt after replies resume. The [past-expiry extension](design/async-job-queue-review.md#live-handler-past-transport-lease-expiry-2026-09-13-utc) rejects stale heartbeat and completion through normal false returns after a same-worker-ID peer reclaims with a fresh token. All five methods per engine pass on native MySQL 8.4.11 and PostgreSQL 16.15; the CI gate now requires five per engine. All ten also pass in the required hosted Docker lane with zero reruns. The direct oracle is not durable caller-identity recovery, and pre-expiry renewal does not prove sustained runtime/listener partitions, shared-pool sizing, replica failover or business-write fencing.
  Runtime transport cleanup now waits up to 15 seconds for both active JDBC connections and connection waiters to drain after coordinator shutdown, before closing the pool. Handler gauges and heartbeat cancellation alone do not certify that an in-flight renewal has exited; preserve this distinction when extending the fault matrix.
  Preserve the shared [MySQL/PostgreSQL restart fixture](design/async-job-queue-review.md#mysql-and-postgresql-restart-matrix-2026-09-12-utc) in its required Docker CI paths, now verified on the exact release. The maintained cases pass on native MySQL 8.0.43 after SIGKILL and PostgreSQL 16.15 after immediate shutdown/WAL recovery, preserving committed rows, rolling back uncommitted input, recovering the original pool and fencing expired owners. The [runtime extension](design/async-job-queue-review.md#runtime-database-restart-recovery-2026-09-12-utc) also verifies stale-result/callback rejection, reclaim and new work through the same coordinator without restart or external hints. Both restart methods now also pass on native MySQL 8.4.11, alongside two pool/session-loss and two worker-JVM crash cases. Preserve their stale-owner and terminal-DONE/missed-callback assertions. The four Docker restart cases and three PostgreSQL listener/session-recovery cases now pass. Broader listener/replica failover, network blackholes and sustained/shared-pool load remain unverified; queue fencing does not undo business effects.
  Preserve the eight [native PostgreSQL 16.15 store groups](design/async-job-queue-review.md#native-postgresql-store-contracts-2026-09-12-utc), including lease expiry during row-lock waits, concurrent retention status/cutoff rechecks and renewal contention with 24 handlers. These are scoped executions of existing contracts, not new maintained tests, a MySQL run or the full CI selection. The 1,000-job smoke with zero poll failures is a local diagnostic, not production capacity. The separate [PostgreSQL timezone matrix](design/async-job-queue-review.md#native-postgresql-timezone-contracts-2026-09-12-utc) now passes 14 existing tests in each UTC and Los Angeles JVM, preserving new-write scheduling, leases and terminal cutoffs across sessions. It does not establish historical timestamp provenance or mixed-worker migration safety; broader replica-failover and network-blackhole gates remain open beyond the passing crash/reconnect fixtures.
  Preserve both [native PostgreSQL pool/session-loss contracts](design/async-job-queue-review.md#native-postgresql-pool-and-session-loss-2026-09-12-utc): actual pool starvation and backend termination with blocked reauthentication retain the replacement lease after recovery, reject stale completion and release runtime capacity. This closes the scoped PostgreSQL session-loss execution gap, not shared-pool sizing, network-partition, replica-failover or multi-host soak requirements; queue-row fencing does not prevent arbitrary business side effects. The two [native PostgreSQL worker-JVM crash cases](design/async-job-queue-review.md#native-postgresql-worker-jvm-crashes-2026-09-12-utc) now verify natural-expiry reclaim and committed-DONE persistence after actual worker termination. Repeated business probes and a missed terminal callback preserve the explicit at-least-once/reconciliation limits; they do not close database-outage or publication-repair gates.
  Preserve [poll recovery despite logging failure](design/async-job-queue-review.md#poll-logging-recovery-2026-09-12-utc): the runtime now guards only its three recovery-path log sites. Real scheduler/executor plus in-memory state verifies completion after transient claim/log failure; this is not database-outage proof. Genuine failure of both scheduling attempts still requires an external hint or restart once scheduling recovers; verify unscheduled-loop alerts rather than claiming a watchdog exists.
  Check at least 5 GiB free before database/build/stress runs. The 2026-09-12 native crash-test server hit ENOSPC during shutdown despite passing worker tests; a separate user-requested monorepo cleanup has since restored about 40 GiB. Keep the safety floor and do not automatically delete unrelated files or caches.
  Preserve the [CI adapter selection correction](design/async-job-queue-quartz-migration.md#required-real-database-and-compatibility-evidence), now verified in the full required hosted job: the dedicated opted-in, zero-rerun selector includes `AssetLocalizeAsyncJobOutputRetryIntegrationTest`, with a workflow regression guard. Its [native PostgreSQL 16.15 method](design/async-job-queue-review.md#native-postgresql-asset-adapter-2026-09-12-utc) and [native MySQL 8.0.43 method](design/async-job-queue-review.md#native-mysql-asset-adapter-2026-09-12-utc) each pass 15 internal scenarios as one JUnit test with no skips/reruns, keeping application state and blobs in HSQL. The [same MySQL method now also passes on native 8.4.11](design/async-job-queue-review.md#native-mysql-84-compatibility-lanes-2026-09-12-utc); the adapter class also passes all 17 selected tests in the required Docker/Linux job; separate adapter databases do not certify atomic admission or full-application database compatibility.
  Preserve the native [MySQL 8.0 JPA/JDBC evidence](design/async-job-queue-review.md#native-mysql-jpajdbc-contracts-2026-09-12-utc) and [PostgreSQL 16.15's 35-test fixture](design/async-job-queue-review.md#native-postgresql-jpajdbc-contracts-2026-09-12-utc); preserve the [native 8.4.11 application and independent-JAR JPA passes](design/async-job-queue-review.md#native-mysql-84-jpa-transactions-2026-09-12-utc) and preserve the full configured CI selection that now passes, without treating injected JDBC outcomes as network recovery or durable admission. These application fixtures do not certify the independent JAR's host-owned JPA matrix. Complete measured shared-pool headroom, network blackhole/replica-failover and sustained multi-host soak; the [native pool/session-loss contracts](design/async-job-queue-review.md#native-mysql-pool-and-session-loss-2026-09-12-utc) and [separate worker-JVM crash cases](design/async-job-queue-review.md#native-mysql-worker-jvm-crashes-2026-09-12-utc) verify narrower recovery/fencing behavior, not those broader gates or exactly-once business effects. Run the opt-in performance smoke and meet the agreed canary/rollback thresholds; smoke throughput is not production capacity. Keep database startup/handshake/address-family failures separate from queue SQL defects instead of masking them with retries, longer sleeps or disabled SSL. Use the [required rollout evidence](design/async-job-queue-quartz-migration.md#required-real-database-and-compatibility-evidence) and dated [verification ledger](design/async-job-queue-review.md) for exact selections and remaining risks.
- TM-02 — Fix stale source-cache identities and usage during extraction without unconditional cache reconciliation. Preserve ordinary cache hits, bound fresh reads to missing identities and leveraging candidates, and test both translation reuse and database read volume before restoring the reverted refresh.
