package com.box.l10n.mojito.service.drop.exporter;

/**
 * Enum of supported {@link DropExporter} supported
 *
 * <p>TODO(P1) In fact an enum will prevent to add customized exporter in a plugin fashion, probably
 * what not we want to do.
 *
 * @author jaurambault
 */
public enum DropExporterType {
  FILE_SYSTEM
}
